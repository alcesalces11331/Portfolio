import React from 'react';

const Project = props => <div className="project-container-body" key={this.props.name}>
    <div className="project-container-detail">
        <p className="project-name" href={this.props.address} >{this.props.name}</p>
        <p className="project-description">{this.props.description}</p>
    </div>
</div>

export default Projects;